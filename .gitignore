__pycache__/
*.pyc
node_modules/
dist/
.venv/

# documentacion de trabajo: no se publica
*.md
!emergence/README.md

# datos generados por --export: se regeneran, no se versionan
client/public/data/*.json
client/public/data/*.jsonl

# log del servidor en vivo: cambia sin parar, no es un resultado
data/*.log

# dataset: solo lo que usa el generador (24K). El derivado son 2.8M
data/raw/MushroomDataset/secondary_data.csv
